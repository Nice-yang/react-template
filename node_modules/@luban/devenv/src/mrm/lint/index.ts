import {install} from "mrm-core";
import MrmTask from "../../types/MrmTask";

const task: MrmTask = function lint() {
    install({"@luban/linter": "^1.9.1"}, {yarn: true});
};
task.description = "Add lint for Luban developer";
export = task;
