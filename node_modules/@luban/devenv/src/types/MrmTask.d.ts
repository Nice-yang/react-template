export interface TaskConfig<T> {
    require(...names: string[]): this;
    defaults(defaultOptions: T): this;
    values(): T;
}

type Task<T = {}> = ((config: TaskConfig<T>) => void) & {description?: string};
export default Task;
