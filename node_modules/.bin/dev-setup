#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/sh" ]; then
  "$basedir/sh"  "$basedir/../@luban/devenv/bin/dev-setup" "$@"
  ret=$?
else 
  sh  "$basedir/../@luban/devenv/bin/dev-setup" "$@"
  ret=$?
fi
exit $ret
